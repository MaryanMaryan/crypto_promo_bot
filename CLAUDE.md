# 🧠 ПАМЯТЬ ПРОЕКТА: Crypto Promo Bot (Оптимизированная версия)

**Версия:** 2.1  
**Последнее обновление:** 2026-01-15

---

## 📋 КРАТКОЕ ОПИСАНИЕ

**Crypto Promo Bot** — Telegram-бот для автоматического мониторинга и оповещения о криптовалютных промоакциях и стейкингах на различных биржах. Система включает множественные методы парсинга с обходом блокировок, включая API, HTML, браузерный парсинг и мониторинг Telegram-каналов.

**Новое в версии 2.0:** Интеллектуальная система умных уведомлений для стейкингов. Fixed стейкинги уведомляют сразу, Flexible ждут стабилизации APR, что предотвращает спам и улучшает качество уведомлений.

## 🚀 ЭКСПРЕСС-НАВИГАЦИЯ: ЧТО ГДЕ НАХОДИТСЯ

### 📁 КЛЮЧЕВЫЕ ФАЙЛЫ ДЛЯ РАЗНЫХ ЗАДАЧ

#### 🔧 **1. РАБОТА С ПРОМОАКЦИЯМИ (НЕ стейкинги)**
ГЛАВНЫЙ ФАЙЛ: bot/parser_service.py (строк ~300)
├─ Метод: check_for_new_promos() - основная логика
├─ Цепочка парсинга: API → HTML → Browser → Telegram
└─ Возвращает: список новых промоакций

ПОДДЕРЖКА:
├─ parsers/universal_parser.py - API парсер
├─ parsers/universal_fallback_parser.py - HTML fallback
├─ parsers/browser_parser.py - браузерный парсинг
└─ parsers/telegram_parser.py - парсинг Telegram

БАЗА ДАННЫХ:
└─ data/models.py → таблица promo_history

#### 💰 **2. РАБОТА СО СТЕЙКИНГАМИ**
ГЛАВНЫЙ ФАЙЛ: parsers/staking_parser.py (строк ~500)
├─ Метод: parse() - парсинг 6+ бирж
├─ Поддерживает: Bybit, Kucoin, OKX, Gate.io, Binance, MEXC
└─ Возвращает: унифицированный формат стейкингов

УМНЫЕ УВЕДОМЛЕНИЯ (версия 2.0):
├─ services/stability_tracker_service.py - логика Fixed/Flexible
├─ Настройки: api_links.flexible_stability_hours (6 часов)
└─ Логика: Fixed - сразу, Flexible - ждет стабилизации

БАЗА ДАННЫХ:
├─ data/models.py → таблица staking_history
└─ data/models.py → таблица staking_snapshots (дельта-изменения)

#### 🤖 **3. TELEGRAM БОТ (команды, интерфейс)**
ГЛАВНЫЙ ФАЙЛ: bot/handlers.py (700+ строк)

Импорты и инициализация (строки ~1–50)
Все импорты (aiogram, модели БД, сервисы, менеджеры прокси/UA/статистики/ротации).
Создание роутера, логгера, parser_service.
Вспомогательные функции для callback (~51–80)safe_answer_callback — безопасный ответ на callback с обработкой таймаутов.
Глобальные хранилища и система навигации (~81–130)
user_selections, navigation_stack, current_stakings_state
Функции push/pop/get/clear_navigation — стек контекстной навигации пользователя (чтобы "Назад" работал правильно).

FSM-состояния (~131–200)
Определение всех StatesGroup:
AddLinkStates (многошаговое добавление ссылки)
IntervalStates, RenameLinkStates, ConfigureParsingStates
Новые: ProxyManagementStates, UserAgentStates, RotationSettingsStates

Клавиатуры главного меню и управления (~201–500)
Множество функций get_*_keyboard():
Главное меню
Управление ссылками (обычное и для стейкинга)
Пагинация стейкингов
Списки ссылок, интервалы, подтверждения, пауза/возобновление
Настройка парсинга (тип, URL, Telegram)
Новые подсистемы: прокси, User-Agent, статистика, ротация, обход блокировок
Клавиатуры "Назад/Отмена"

Основные команды бота (~501–600)
/start — очистка навигации + главное меню
/help и кнопка "Помощь"
Прямой ввод URL (предупреждение использовать "Добавить ссылку")

Обработчики главного меню (~601–700)
"Список ссылок" — вывод всех ссылок с иконками категории/типа/статуса
"Проверить всё" — ручная проверка активных ссылок
"Обход блокировок" — вход в подменю прокси/UA/ротации/статистики

Процесс добавления ссылки (самый большой блок, ~701–1500)
Начало: выбор категории
Ввод названия
Выбор типа парсинга (combined/api/html/browser/telegram)
Ввод соответствующих URL (API/HTML)
Специфика для Telegram: канал + ключевые слова
Опционально: пример ссылки (для шаблона)
Выбор интервала
Множество "Назад" обработчиков для каждого шага

Настройка парсинга существующих ссылок (~1501–1800)
Изменение типа парсинга
Редактирование API/HTML URL
Редактирование Telegram-канала и ключевых слов

Управление прокси (~1801–2100)
Меню прокси
Список, добавление (адрес + протокол), тестирование всех, статистика, удаление

Управление User-Agent (~2101–2300)
Список, статистика, добавление вручную, генерация новых

Статистика системы (~2301–2500)
Общая, по биржам, лучшие комбинации, статус ротации

Настройки ротации (~2501–2700)
Текущие настройки, изменение интервала, вкл/выкл автооптимизации, принудительная ротация, очистка данных

Общие отмены и возврат в меню (~2701–2800)
Обработчики отмены для новых подсистем (прокси/UA/статистика/ротация) → возврат в "Обход блокировок"

Вспомогательные функции в конце (~2801–конец)
_format_timestamp — форматирование времени

ПОДДЕРЖКА:
├─ bot/keyboards.py - клавиатуры Telegram
├─ bot/bot_manager.py - управление инстансом бота
└─ bot/notification_service.py - форматирование уведомлений

#### 📊 **4. МОНИТОРИНГ TELEGRAM КАНАЛОВ**
ГЛАВНЫЙ ФАЙЛ: services/telegram_monitor.py
├─ Мониторинг каналов через Telethon
├─ Балансировка между аккаунтами
└─ Поиск по ключевым словам

ПОДДЕРЖКА:
├─ parsers/telegram_parser.py - парсинг сообщений
├─ data/models.py → таблица telegram_accounts
└─ data/models.py → таблица telegram_messages

#### 🛡️ **5. СИСТЕМА ОБХОДА БЛОКИРОВОК**
ПРОКСИ:
└─ utils/proxy_manager.py - управление прокси серверами

USER-AGENT:
└─ utils/user_agent_manager.py - ротация User-Agent

РОТАЦИЯ:
└─ utils/rotation_manager.py - автоматическая ротация IP/UA

СТАТИСТИКА:
└─ utils/statistics_manager.py - отслеживание успешности парсинга

#### 🗄️ **6. БАЗА ДАННЫХ И МОДЕЛИ**
ГЛАВНЫЙ ФАЙЛ: data/models.py (ВСЕ модели)
├─ ApiLink - настройки парсинга ссылок
├─ PromoHistory - история промоакций
├─ StakingHistory - история стейкингов (с полями умных уведомлений)
├─ StakingSnapshot - снимки стейкингов
├─ TelegramAccount - аккаунты для парсинга
├─ ProxyServer - прокси серверы
└─ UserLinkSubscription - подписки пользователей

ИНИЦИАЛИЗАЦИЯ:
└─ data/database.py - создание БД, миграции

#### ⚙️ **7. КОНФИГУРАЦИЯ И ЗАПУСК**
ТОЧКА ВХОДА: main.py (НЕ трогать)
├─ Метод: smart_auto_check() - автопроверка раз в минуту
├─ Метод: setup_scheduler() - настройка планировщика
└─ Метод: start() - запуск бота

КОНФИГУРАЦИЯ: config.py (НЕ трогать)
├─ Загрузка .env файла
├─ Валидация токенов
└─ Настройка интервалов

ЗАВИСИМОСТИ: requirements.txt
└─ aiogram, requests, beautifulsoup4, sqlalchemy, playwright, telethon

#### 🧪 **8. ТЕСТИРОВАНИЕ И РАЗРАБОТКА**
ТЕСТЫ: dev/tests/
├─ test_.py - unit тесты
├─ check_.py - проверки функциональности
└─ debug_*.py - отладка

УТИЛИТЫ: dev/scripts/
├─ add_.py - добавление данных
├─ setup_.py - настройка
└─ fix_*.py - исправления

ДОКУМЕНТАЦИЯ: dev/docs/
└─ *.md файлы с описанием

TODO: dev/todos/
└─ TODO_*.md - списки задач

---

## 🎯 СТРАТЕГИЯ ОПТИМИЗАЦИИ ТОКЕНОВ

### **ПРАВИЛО 1: ЧИТАЙ ТОЛЬКО НУЖНОЕ**
Если задача: Читай ТОЛЬКО:
├─ Добавить парсинг новой биржи → parsers/
├─ Изменить логику промоакций → bot/parser_service.py
├─ Добавить новую команду бота → bot/handlers.py
├─ Настроить умные уведомления → services/
├─ Исправить проблему с БД → data/
├─ Настроить прокси → utils/proxy_manager.py
└─ Тестирование → dev/tests/

### **ПРАВИЛО 2: ИЕРАРХИЯ ВАЖНОСТИ ФАЙЛОВ**
ВЫСОКИЙ ПРИОРИТЕТ (часто нужны):

bot/parser_service.py # ядро парсинга

parsers/staking_parser.py # парсинг стейкингов

bot/handlers.py # команды бота

data/models.py # структура БД

СРЕДНИЙ ПРИОРИТЕТ:
5. services/stability_tracker_service.py # умные уведомления
6. bot/notification_service.py # форматирование уведомлений
7. services/telegram_monitor.py # мониторинг Telegram

НИЗКИЙ ПРИОРИТЕТ (редко нужны):
8. utils/ (все менеджеры) # утилиты
9. parsers/ (остальные парсеры) # вспомогательные парсеры
10. dev/ # разработка и тесты

### **ПРАВИЛО 3: АЛГОРИТМ ПОИСКА РЕШЕНИЯ**
ШАГ 1: Определи тип задачи
├─ Парсинг? → Смотри РАЗДЕЛ 1 или 2
├─ Команда бота? → Смотри РАЗДЕЛ 3
├─ Уведомления? → Смотри РАЗДЕЛ 7
└─ База данных? → Смотри РАЗДЕЛ 6

ШАГ 2: Открой соответствующий ГЛАВНЫЙ ФАЙЛ

ШАГ 3: Если не найдешь ответ → проверь ПОДДЕРЖКУ

ШАГ 4: Если все еще не ясно → проверь utils/ или dev/tests/

---

## 🔧 ГЛАВНЫЕ ФУНКЦИИ

### 1. **main.py - CryptoPromoBot**

#### Основные методы:
- `init_services()` - инициализация всех сервисов
- `smart_auto_check()` - автопроверка активных ссылок (каждую минуту)
- `manual_check_all_links(chat_id)` - ручная проверка всех активных ссылок
- `force_check_specific_link(chat_id, link_id)` - принудительная проверка одной ссылки
- `setup_scheduler()` - настройка планировщика
- `start()` - запуск бота
- `shutdown()` - корректное завершение

**Логика проверки:**
- Проверяет ТОЛЬКО активные ссылки (`is_active=True`)
- Для каждой ссылки проверяет `check_interval` (время с последней проверки)
- Для `category='staking'` использует `parse_staking_link()`
- Для обычных промоакций использует `check_for_new_promos()`
- Обновляет `last_checked` после проверки

### 2. **bot/parser_service.py - ParserService**

#### Основные методы:
- `check_for_new_promos(link_id, url)` - проверка новых промоакций
- `parse_staking_link(link_id, api_url, exchange, page_url, min_apr)` - парсинг стейкингов

**Цепочка парсинга промоакций:**
1. Пытается API парсер (`universal_parser.py`)
2. Если неудача → пытается HTML fallback (`universal_fallback_parser.py`)
3. Если неудача → пытается browser парсер (`browser_parser.py`)
4. Если неудача → пытается Telegram парсер (`telegram_parser.py`)

### 3. **parsers/staking_parser.py - StakingParser**

**Поддерживаемые биржи:**
- Bybit (POST запросы с payload)
- Kucoin (GET запросы)
- OKX (GET запросы, группировка по проектам)
- Gate.io (GET запросы, множественная пагинация)
- Binance (GET запросы)
- MEXC (GET запросы)

**Автоопределение биржи:** по домену в URL (bybit.com, kucoin.com, okx.com, gate.io, binance.com, mexc.com)

**Унифицированный формат стейкинга:**
```python
{
    'exchange': 'bybit',
    'coin': 'BTC',
    'apr': 5.5,
    'duration_days': 30,
    'lock_type': 'Fixed',
    'status': 'ONGOING',
    'product_id': 'unique_id',
    'fill_percentage': 75.5,  # опционально
    'token_price_usd': 50000,  # опционально
    'value_usd': 1000000,  # опционально
    'start_time': datetime,
    'end_time': datetime
}
```

### 4. **bot/notification_service.py - NotificationService**

#### Основные методы:
- `send_bulk_notifications(chat_id, promos)` - отправка промоакций
- `format_new_staking(staking, page_url)` - форматирование уведомления о стейкинге
- `format_okx_project(pools, page_url)` - форматирование группы пулов OKX

**Формат уведомлений:**
- HTML разметка с эмодзи
- Цветовая индикация заполненности (🟢🟡🔴)
- Ссылки на страницы
- Группировка для OKX

### 5. **services/telegram_monitor.py - TelegramMonitor**

#### Функционал:
- Мониторинг Telegram каналов через Telethon
- Поиск ключевых слов в сообщениях
- Балансировка нагрузки между несколькими Telegram аккаунтами
- Сохранение сообщений в БД

### 6. **services/staking_snapshot_service.py - StakingSnapshotService**

#### Функционал:
- Создание снимков стейкингов каждый час
- Расчет дельт изменений (APR, заполненность, цена, стоимость)
- Интеллектуальные уведомления о значимых изменениях

### 7. **services/stability_tracker_service.py - StabilityTrackerService**


🆘 КРИТИЧНО ВАЖНЫЕ ФАЙЛЫ (НЕ ПЕРЕМЕЩАТЬ!)
🚨 ЗАЩИЩЕННЫЕ ФАЙЛЫ:
1. main.py          # Точка входа
2. config.py        # Конфигурация
3. bybit_coin_mapping.py # Маппинг для Bybit
📁 ПРАВИЛЬНАЯ СТРУКТУРА:
sessions/           # Telegram сессии (НЕ в корне!)
├─ account1.session
└─ account2.session

backups/            # Бэкапы рабочих версий

dev/                # ТОЛЬКО для разработки
├─ tests/           # тесты
├─ scripts/         # утилиты
├─ docs/            # документация
└─ todos/           # TODO списки
🔄 ПОСЛЕДНИЕ ИЗМЕНЕНИЯ
2026-01-15 (v2.1):

✅ Создана оптимизированная версия памяти

✅ Добавлена интеллектуальная навигация

✅ Определены приоритеты файлов

✅ Созданы правила оптимизации токенов

2026-01-13 (v2.0):

✅ Система умных уведомлений (Fixed/Flexible/Combined)

✅ StabilityTrackerService

✅ Обновлены модели БД

✅ Миграционные скрипты

2026-01-13 (v1.0):

✅ Первая версия памяти проекта

✅ Описание архитектуры

✅ Структура проекта

💡 ИНСТРУКЦИЯ ДЛЯ CLAUDE
КОГДА ТЕБЕ НУЖНО ЧТО-ТО СДЕЛАТЬ:
Сначала посмотри этот файл → найди нужный раздел

Открой только указанные ГЛАВНЫЕ ФАЙЛЫ

Не читай весь проект → используй стратегию оптимизации

Если не уверен → спроси у меня, какие файлы нужны

ПРИМЕРЫ:
Вопрос: "Как изменить интервал проверки ссылки?"
Ответ: Смотри РАЗДЕЛ 3 → bot/handlers.py → команда изменения интервала

Вопрос: "Как добавить парсинг новой биржи?"
Ответ: Смотри РАЗДЕЛ 2 → parsers/staking_parser.py → инструкция в файле

Вопрос: "Почему не работают уведомления?"
Ответ: Смотри РАЗДЕЛ 7 → services/stability_tracker_service.py → логика Fixed/Flexible

КОГДА ОБРАЩАТЬСЯ КО МНЕ !!!
Если не найдешь нужный файл в навигации

Если нужно прочитать несколько больших файлов

Для получения конкретных файлов из проекта

При изменении защищенных файлов (main.py, config.py)