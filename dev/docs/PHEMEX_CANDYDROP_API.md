# Phemex Candy Drop API Integration

## Найденные API Endpoints

Base URL: `https://api10.phemex.com`

### 1. Получение активностей Candy Drop

| Статус | Endpoint |
|--------|----------|
| Upcoming (предстоящие) | `/phemex-activity/public/md/candydrop/activities?status=0` |
| Active (активные) | `/phemex-activity/public/md/candydrop/activities?status=1` |
| Ended (завершённые) | `/phemex-activity/public/md/candydrop/activities?status=2` |

### Формат ответа:

```json
{
    "code": 0,
    "msg": "OK",
    "data": [
        {
            "activityId": 820,
            "types": [3],
            "activityName": "IMU",
            "description": "IMU",
            "status": 0,
            "rewardAmount": 500000000000000,
            "currencyType": "1117",
            "participants": 18639,
            "startTime": 1768903200000,
            "endTime": 1769076000000,
            "signBeginTime": 1768903200000,
            "signEndTime": 1768903200000
        }
    ]
}
```

### Поля ответа:

| Поле | Описание |
|------|----------|
| `activityId` | Уникальный ID акции |
| `types` | Тип акции (обычно `[3]` для candy drop) |
| `activityName` | Название токена/акции (uppercase рекомендуется) |
| `description` | Описание акции |
| `status` | Статус: 0=upcoming, 1=active, 2=ended |
| `rewardAmount` | Сумма наград (делить на 10^8) |
| `currencyType` | Код валюты |
| `participants` | Количество участников |
| `startTime` | Время начала (Unix timestamp в ms) |
| `endTime` | Время окончания (может быть в ms или секундах!) |
| `signBeginTime` | Начало регистрации |
| `signEndTime` | Конец регистрации |

## Особенности API

⚠️ **ВАЖНО:** API возвращает 403 при прямых HTTP запросах!

Требуется:
- Использовать Playwright для загрузки страницы
- Перехватывать XHR запросы через network events
- API работает только из контекста браузера с cookies

## Интеграция в бот

### Файлы парсера:
- `parsers/phemex_candydrop_parser.py` - основной парсер

### Конфигурация:
- Добавлен в `PARSERS_CONFIG` в `bot/handlers.py`
- Поддержка в `notification_service.py`

### Автодетект URL:
```python
# Детектит по паттерну:
'phemex.com' + ('candy-drop' | 'candydrop' | '/events/candy')
```

## Тестирование

### Тест парсера:
```bash
cd crypto_promo_bot
python -c "
import asyncio
import sys
sys.path.insert(0, '.')
from parsers.phemex_candydrop_parser import PhemexCandydropParser, test_parser
asyncio.run(test_parser())
"
```

### Ожидаемый вывод:
```
Phemex Candy Drop: найдено N проектов

🎁 IMU (upcoming)
   Описание: IMU
   Пул: 5,000,000 токенов
   Участников: 18,747
   Начало: 2026-01-20 12:00
   Конец: 2026-01-22 12:00
   URL: https://phemex.com/events/candy-drop#820
...
```

## Добавление ссылки в бота

1. В боте выбрать: **Управление ссылками** → **Добавить ссылку**
2. Категория: **Launches** или **Airdrop**
3. URL: `https://phemex.com/events/candy-drop`
4. Парсер автоматически определится как **Phemex Candy Drop** 🍬

## Формат уведомлений

Уведомления форматируются аналогично другим Candy Drop (Bitget CandyBomb):
- Токен и описание
- Пул наград в токенах
- Количество участников  
- Статус (Активный/Предстоящий/Завершён)
- Оставшееся время
- Ссылка на участие
