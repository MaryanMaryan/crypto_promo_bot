# Расчёт профита для OKX Flash Earn

## Обзор изменений

Добавлен динамический расчёт потенциального профита для OKX Flash Earn стейкингов с учётом оставшегося времени до конца промоакции.

## Логика работы

### Ключевая особенность
OKX Flash Earn отличается от обычных flexible стейкингов тем, что:
- Промоакция имеет фиксированное время окончания
- Пользователи обычно держат средства до конца промоакции для получения наград
- Поэтому профит рассчитывается не за день, а за **весь оставшийся период**

### Алгоритм расчёта

1. **Определение оставшегося времени:**
   ```python
   remaining_days = countdown / 1000 / 86400  # countdown в миллисекундах
   remaining_days = max(1, int(remaining_days))  # минимум 1 день
   ```

2. **Расчёт профита:**
   ```python
   earnings_tokens = (user_limit * apr / 100) * (remaining_days / 365)
   earnings_usd = earnings_tokens * token_price
   ```

3. **Динамическое обновление:**
   - При каждом запросе страницы используется актуальное значение `countdown`
   - Профит автоматически пересчитывается с учётом оставшегося времени

## Примеры

### Пример 1: Начало промоакции (10 дней до конца)
```
Входные данные:
- Лимит: 1000 USDT
- APR: 10%
- Осталось: 10 дней
- Цена: $1

Расчёт:
- Годовой заработок: 1000 * 10% = 100 USDT
- За 10 дней: 100 * (10/365) = 2.74 USDT
- Отображение: "Профит: 2.74 USDT ($3) за 10 дней"
```

### Пример 2: Конец промоакции (3 дня до конца)
```
Входные данные:
- Лимит: 1000 USDT
- APR: 10%
- Осталось: 3 дня
- Цена: $1

Расчёт:
- Годовой заработок: 1000 * 10% = 100 USDT
- За 3 дня: 100 * (3/365) = 0.82 USDT
- Отображение: "Профит: 0.8219 USDT ($0.82) за 3 дня"
```

## Форматирование вывода

### Структура отображения
```
💎 ДОСТУПНЫЕ ПУЛЫ:
├ ETH: 3.77% APR | Лимит: 1.50 (~$4,902)
│  └ Профит: 0.0009 ETH ($3) за 6 дней
├ USDG: 2.46% APR | Лимит: 5,000.00 (~$5,000)
│  └ Профит: 2.02 USDG ($2) за 6 дней
└ BTC: 1.73% APR | Лимит: 0.05 (~$4,739)
   └ Профит: 0.000014 BTC ($1) за 6 дней
```

### Правила форматирования USD
- Если `earnings_usd >= 1`: отображается как целое число `$X`
- Если `earnings_usd < 1`: отображается с 2 знаками после запятой `$0.XX`

## Технические детали

### Изменённые файлы
- `bot/notification_service.py` - функция `format_okx_flash_earn_page()`
- `bot/notification_service.py` - функция `calculate_staking_earnings()` (улучшено форматирование USD)

### Улучшения в calculate_staking_earnings()
Добавлено умное форматирование USD:
```python
if earnings_usd:
    if earnings_usd >= 1:
        usd_str = f"${earnings_usd:,.0f}"
    else:
        usd_str = f"${earnings_usd:.2f}"
```

Это решает проблему, когда малые суммы (< $1) отображались как `($0)` вместо `($0.XX)`.

## Тестирование

Созданы тестовые скрипты:
- `dev/test_okx_profit.py` - основной тест с реалистичными данными
- Ручные тесты в консоли для проверки математики

Все тесты пройдены успешно ✅

## Дата внедрения
16 января 2026
