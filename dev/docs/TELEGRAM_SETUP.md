# Настройка Telegram-парсера

Telegram-парсер позволяет боту автоматически мониторить указанные Telegram-каналы и находить промоакции по ключевым словам.

## 📋 Содержание
1. [Получение Telegram API ключей](#получение-telegram-api-ключей)
2. [Настройка конфигурации](#настройка-конфигурации)
3. [Установка зависимостей](#установка-зависимостей)
4. [Первый запуск](#первый-запуск)
5. [Использование](#использование)
6. [Управление каналами](#управление-каналами)
7. [Устранение неполадок](#устранение-неполадок)

---

## 🔑 Получение Telegram API ключей

### Шаг 1: Создание приложения

1. Откройте **https://my.telegram.org/apps**
2. Войдите с помощью вашего номера телефона
3. Нажмите **"Create new application"**
4. Заполните форму:
   - **App title**: `Crypto Promo Bot` (или любое название)
   - **Short name**: `crypto_bot` (или любое короткое имя)
   - **Platform**: Выберите `Other`
   - **Description**: (опционально) краткое описание

5. Нажмите **"Create application"**

### Шаг 2: Сохранение ключей

После создания вы получите:
- **API ID** (app_id) - число вида `12345678`
- **API hash** (app_hash) - строка вида `0123456789abcdef0123456789abcdef`

⚠️ **ВАЖНО:** Никому не передавайте эти ключи!

---

## ⚙️ Настройка конфигурации

### 1. Откройте файл `.env`

Если файла `.env` нет - скопируйте `.env.example`:
```bash
cp .env.example .env
```

### 2. Добавьте ключи Telegram API

Откройте `.env` и добавьте/обновите следующие строки:

```env
# =============================================================================
# TELEGRAM PARSER CONFIGURATION
# =============================================================================

# Включить Telegram-парсер
TELEGRAM_PARSER_ENABLED=true

# Telegram API Credentials (получите на https://my.telegram.org/apps)
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=0123456789abcdef0123456789abcdef
```

Замените:
- `12345678` на ваш **API ID**
- `0123456789abcdef0123456789abcdef` на ваш **API hash**

### 3. Сохраните файл

---

## 📦 Установка зависимостей

```bash
pip install -r requirements.txt
```

Telethon будет установлен автоматически (версия 1.34.0).

---

## 🚀 Первый запуск

### 1. Запустите бота

```bash
python main.py
```

### 2. Авторизация в Telegram

При первом запуске Telethon попросит авторизоваться:

```
Please enter your phone (or bot token): +380xxxxxxxxx
```

1. Введите номер телефона в международном формате: `+380xxxxxxxxx`
2. Telegram отправит вам код подтверждения
3. Введите полученный код
4. Если у вас включена двухфакторная аутентификация - введите пароль

✅ **Готово!** Сессия сохранится в файле `telegram_parser_session.session`

⚠️ **Не удаляйте этот файл** - иначе придется авторизовываться заново.

---

## 💡 Использование

### Открытие меню Telegram-каналов

1. Запустите бота
2. Нажмите кнопку **"📡 Telegram каналы"** в главном меню

### Добавление первого канала

1. Нажмите **"➕ Добавить канал"**
2. Введите username канала (примеры):
   - `@binance_announcements`
   - `binance_announcements`
   - `https://t.me/binance_announcements`

3. Введите ключевые слова для поиска (через запятую):
   ```
   airdrop, промо, campaign, giveaway
   ```

4. ✅ Готово! Бот начнет мониторить канал.

### Как работает мониторинг

1. Бот подключается к Telegram через Telethon
2. Слушает новые сообщения в добавленных каналах
3. Ищет ключевые слова в тексте сообщений
4. При совпадении:
   - Сохраняет сообщение в БД
   - Отправляет вам уведомление
   - Извлекает ссылки и даты

---

## 🎛️ Управление каналами

### Просмотр списка каналов

Нажмите **"📋 Список каналов"**

Вы увидите:
- Все добавленные каналы
- Статус (активен ✅ / приостановлен ⏸️)
- Количество ключевых слов

### Управление конкретным каналом

Выберите канал из списка, откроется меню:

**1. ⏸️ Приостановить / ▶️ Возобновить**
- Временно отключить мониторинг канала

**2. 🔑 Управление словами**
- **➕ Добавить слово** - добавить новые ключевые слова
- **➖ Удалить слово** - удалить существующие

**3. 🗑️ Удалить канал**
- Полностью удалить канал и все его сообщения

### Просмотр статистики

Нажмите **"📊 Статистика"**

Показывает:
- Общее количество каналов
- Количество активных
- Найденных сообщений
- Топ-5 каналов по сообщениям

---

## 🔧 Устранение неполадок

### Ошибка: "Telegram-парсер отключен в конфигурации"

**Причина:** `TELEGRAM_PARSER_ENABLED=false` в `.env`

**Решение:**
1. Откройте `.env`
2. Измените на `TELEGRAM_PARSER_ENABLED=true`
3. Перезапустите бота

---

### Ошибка: "TELEGRAM_API_ID не установлен"

**Причина:** Не указаны ключи в `.env`

**Решение:**
1. Получите ключи на https://my.telegram.org/apps
2. Добавьте их в `.env`:
   ```env
   TELEGRAM_API_ID=12345678
   TELEGRAM_API_HASH=0123456789abcdef0123456789abcdef
   ```
3. Перезапустите бота

---

### Ошибка: "Сессия недействительна"

**Причина:** Файл сессии поврежден или устарел

**Решение:**
1. Удалите файл `telegram_parser_session.session`
2. Перезапустите бота
3. Пройдите авторизацию заново

---

### Ошибка: "Номер телефона заблокирован"

**Причина:** Telegram заблокировал ваш номер

**Решение:**
- Обратитесь в поддержку Telegram
- Используйте другой номер телефона
- Проверьте, не нарушали ли вы правила Telegram

---

### Ошибка: "Flood Wait: необходимо подождать X секунд"

**Причина:** Превышен лимит запросов к Telegram API

**Решение:**
- Подождите указанное время
- Бот автоматически подождет, если ожидание < 5 минут
- Уменьшите частоту проверок каналов

---

### Бот не получает новые сообщения

**Проверьте:**

1. **Канал активен?**
   - Откройте канал в списке
   - Проверьте статус (должен быть ✅)

2. **Ключевые слова правильные?**
   - Проверьте список ключевых слов
   - Убедитесь, что слова написаны правильно
   - Попробуйте добавить больше синонимов

3. **Бот запущен?**
   - Проверьте логи: должно быть `📡 Telegram Monitor запущен`

4. **Канал публичный?**
   - Telegram-парсер работает только с публичными каналами
   - У канала должен быть username (@channel_name)

---

## 📚 Дополнительная информация

### Лимиты Telegram API

- **Запросы**: ~20 запросов/секунду
- **Сообщения**: ~30 сообщений/секунду
- **Подключений**: 5 одновременных соединений с одного аккаунта

**Рекомендации:**
- Не добавляйте более 50 каналов
- Используйте специфичные ключевые слова
- Избегайте частых проверок архива

---

### Приватность и безопасность

✅ **Безопасно:**
- Бот использует официальный Telegram API
- Данные не передаются третьим лицам
- Сессия хранится локально

⚠️ **Будьте осторожны:**
- Не публикуйте файл `.env` или `.session`
- Не передавайте API ключи другим
- Регулярно проверяйте активные сессии: https://my.telegram.org/auth

---

### Структура БД

**Таблица `telegram_channels`:**
- `channel_username` - @username канала
- `keywords` - JSON массив ключевых слов
- `is_active` - статус мониторинга
- `total_messages_found` - количество найденных сообщений

**Таблица `telegram_messages`:**
- `channel_id` - ID канала
- `message_id` - ID сообщения в Telegram
- `message_text` - полный текст
- `matched_keywords` - совпавшие ключевые слова
- `extracted_links` - извлеченные ссылки
- `notification_sent` - отправлено ли уведомление

---

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи бота: `python main.py`
2. Изучите раздел "Устранение неполадок" выше
3. Проверьте настройки в `.env`
4. Убедитесь, что используете актуальную версию

---

**Успешной работы с Telegram-парсером!** 🚀
