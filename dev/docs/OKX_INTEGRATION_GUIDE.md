# Руководство по интеграции OKX Flash Earn

## Как добавить OKX Flash Earn в бота

### Шаг 1: Добавление через бота

1. Откройте бота и нажмите "➕ Добавить ссылку"
2. Выберите категорию **💰 Стейкинг** (ОБЯЗАТЕЛЬНО!)
3. Введите название: `OKX Flash Earn` (или любое другое)

### Шаг 2: Добавление API URL

**API URL:** `https://www.okx.com/priapi/v3/stake-earn/projects`

### Шаг 3: Пропустить HTML URL

HTML URL не нужен для OKX, нажмите "⏭️ Пропустить"

### Шаг 4: Добавить страницу стейкинга (опционально)

**Page URL:** `https://www.okx.com/ru/earn/flash-earn`

### Шаг 5: Настройка фильтров (опционально)

- **Минимальный APR:** 10% (или оставьте пустым)
- **Отслеживать заполненность:** Нет (т.к. OKX не предоставляет общий лимит пула)
- **Интервал проверки:** 300 секунд (5 минут) или любой другой

## Что будет отслеживаться

Бот будет мониторить OKX Flash Earn и уведомлять вас о:

✅ **Новых проектах стейкинга** (например, новый проект HUMA)
- Каждый проект содержит несколько пулов (OKB, BTC, ETH и т.д.)
- Уведомление приходит один раз на весь проект

✅ **Изменениях APR**
- Когда APR любого пула поднимается выше порога (по умолчанию 10%)

## Формат уведомлений

Пример уведомления о новом проекте:

```
🎉 НОВЫЙ СТЕЙКИНГ!

🏢 Биржа: OKX
📌 Монета: OKB
💰 APR: 4.37%
🎁 Награда: HUMA

📊 Заполненность: 2740643.85 OKB
📅 Окончание: 2026-01-08 12:00:00

💵 Лимит на пользователя: 50.0 OKB ($5670.00)
🔗 https://www.okx.com/ru/earn/flash-earn
```

## Технические детали

### Что парсится из API OKX

- ✅ Активные проекты (`ongoingProjects`)
- ✅ Все пулы проекта (OKB, BTC, ETH и т.д.)
- ✅ APR для каждого пула
- ✅ Монета награды (rewardDetails)
- ✅ Текущая заполненность пула (poolAccumulatedPurchaseAmount)
- ✅ Лимит на пользователя (upperLimit для VIP 0)
- ✅ Дата окончания проекта (endTime)
- ✅ Цены токенов через CoinGecko API

### Что НЕ доступно в API OKX

- ❌ Общий лимит пула (maxStakingLimit - это лимит для ОДНОГО пользователя VIP уровня, а не пула)
- ❌ Процент заполненности пула (нет данных об общем лимите)
- ❌ Точная дата начала (startTime есть, но не всегда актуальна)

### Структура парсинга

OKX Flash Earn парсится через `StakingParser` с методом `_parse_okx()`:

```python
from parsers.staking_parser import StakingParser

parser = StakingParser(
    api_url="https://www.okx.com/priapi/v3/stake-earn/projects",
    exchange_name="OKX"
)

stakings = parser.parse()
```

## Тестирование

Для тестирования парсера запустите:

```bash
python dev/tests/test_okx_parser.py
```

## Устранение проблем

### Проблема: Получаю уведомления вида "Okx Promo okx_51"

**Решение:** Вы добавили ссылку с неправильной категорией. Удалите старую ссылку и добавьте заново, выбрав категорию **💰 Стейкинг**.

### Проблема: Не показывается заполненность пулов

**Ответ:** Это нормально! OKX API не предоставляет информацию об общем лимите пула, поэтому процент заполненности невозможно рассчитать. Показывается только текущая заполненность в токенах.

### Проблема: APR ниже ожидаемого

**Ответ:** OKX Flash Earn обычно имеет невысокий APR (2-5%). Это нормально для данного типа продукта. Вы можете настроить фильтр APR на более низкое значение.
