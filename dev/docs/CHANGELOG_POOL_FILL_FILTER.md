# Изменения: Фильтр заполненности пулов стейкингов

## Дата: 16 января 2026

## Проблема
В списке текущих стейкингов (особенно для GateStaking) показывались стейкинги с практически полностью заполненными пулами (99.98%, 99.99%, 99.95%), в которые невозможно зайти из-за отсутствия свободных мест.

## Решение
Добавлен настраиваемый фильтр по максимальной заполненности пула.

## Изменения

### 1. Новая настройка в `config.py`
Добавлена переменная окружения `MAX_POOL_FILL_PERCENTAGE` (по умолчанию 90.0%):

```python
# =============================================================================
# STAKING CONFIGURATION
# =============================================================================
# Максимальная заполненность пула (%) для отображения стейкингов
# Стейкинги с заполненностью выше этого порога не будут показываться
MAX_POOL_FILL_PERCENTAGE = float(os.getenv('MAX_POOL_FILL_PERCENTAGE', '90.0'))
```

### 2. Обновлен фильтр в `services/staking_snapshot_service.py`
- Добавлен фильтр по заполненности пула (используется `MAX_POOL_FILL_PERCENTAGE`)
- Добавлено детальное логирование статистики фильтрации
- Обновлен алерт "Пул почти заполнен" для работы с настраиваемым порогом

### 3. Логирование
Теперь в логах отображается подробная статистика фильтрации:
```
📊 Статистика для GateStaking:
   ├─ Всего стейкингов: 15
   ├─ Отфильтровано по заполненности (≥90.0%): 5
   ├─ Отфильтровано по времени окончания: 2
   ├─ Отфильтровано по APR (<50.0%): 3
   └─ Итого показано: 5
```

## Как использовать

### Способ 1: Использовать значение по умолчанию (90%)
Ничего не нужно делать - фильтр уже работает с порогом 90%.

### Способ 2: Настроить свой порог
Добавьте в файл `.env` переменную:
```env
# Показывать только стейкинги с заполненностью ниже 85%
MAX_POOL_FILL_PERCENTAGE=85.0

# Или для менее строгой фильтрации (до 95%)
MAX_POOL_FILL_PERCENTAGE=95.0
```

## Примеры

### До изменений:
```
💰 BREV | 499.0% APR | Flexible
💎 ЗАПОЛНЕННОСТЬ ПУЛА:
   ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░ 99.98%
   • Осталось: 99.85 BREV  ❌ Зайти невозможно!
```

### После изменений (с порогом 90%):
Такие стейкинги НЕ показываются в списке, отображаются только те, где есть реальная возможность зайти.

## Преимущества
1. ✅ Показываются только актуальные стейкинги с доступными местами
2. ✅ Настраиваемый порог - можно легко изменить через `.env`
3. ✅ Детальная статистика фильтрации в логах
4. ✅ Стейкинги без данных о заполненности продолжают показываться

## Дополнительная информация
- Фильтр применяется автоматически при просмотре "Текущих стейкингов"
- Не влияет на уведомления о новых стейкингах
- Совместим с фильтром по APR
