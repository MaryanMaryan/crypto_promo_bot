# FIX: MEXC Announcements Browser Parsing

## Проблема
При принудительной проверке ссылки **Mexc0%Fee** (`https://www.mexc.co/announcements/all`) бот не находил ключевые слова и не присылал уведомления.

## Причина
Страница MEXC анонсов использует **Next.js (React)** и загружает контент динамически через JavaScript. Обычный HTTP-парсер получал только пустой HTML-скелет без данных.

## Решение
Добавлена поддержка **браузерного парсинга через Playwright** в `AnnouncementParser`:

### Изменения в коде:

1. **parsers/announcement_parser.py**
   - Добавлен параметр `use_browser` в метод `parse()`
   - Добавлен метод `_fetch_with_browser()` для загрузки страниц через Playwright
   - Интегрирована поддержка playwright-stealth для обхода детекции
   - Использует систему ротации прокси и User-Agent

2. **bot/parser_service.py**
   - Обновлен метод `check_announcement_link()` для передачи флага `use_browser`
   - Флаг определяется автоматически: `use_browser = link.parsing_type == 'browser'`

3. **База данных**
   - Обновлена ссылка Mexc0%FEE: установлен `parsing_type='browser'`

## Настройка ссылки MEXC

```python
# Ссылка настроена через бота или вручную:
ID: 14
Название: Mexc0%FEE
URL: https://www.mexc.co/announcements/all
Категория: announcement
Тип парсинга: browser  # ← КРИТИЧНО для динамических страниц
Стратегия: any_keyword
Ключевые слова: ["0% fee"]
```

## Как работает

1. При принудительной проверке бот определяет, что `parsing_type='browser'`
2. `AnnouncementParser` запускает Playwright (Chromium)
3. **Попытка 1**: Загрузка с прокси (если доступен)
   - Если успешно → возвращает HTML
   - Если таймаут/ошибка → переходит к попытке 2
4. **Попытка 2 (FALLBACK)**: Загрузка БЕЗ прокси
   - Обеспечивает надежность даже если прокси не работает
5. Применяется stealth mode для обхода детекции (опционально)
6. Страница загружается с выполнением JavaScript (5 сек ожидания)
7. Извлекается полный HTML с данными
8. Поиск ключевых слов в загруженном контенте

## Результат

✅ Браузерный парсер успешно загружает страницу MEXC (385KB HTML)
✅ Ключевые слова находятся корректно
✅ Уведомления будут приходить при обнаружении изменений

## Требования

- `playwright>=1.56.0`
- `playwright-stealth>=2.0.0`
- Установленные браузеры Chromium: `playwright install chromium`

## Применение для других бирж

Для любой биржи с динамическим контентом:

1. Создайте ссылку с категорией `announcement`
2. Установите `parsing_type='browser'`
3. Выберите стратегию и ключевые слова
4. Бот автоматически будет использовать браузерный парсинг

## Логи

При принудительной проверке теперь видны логи:
```
🔍 AnnouncementParser: Начало парсинга
   Браузерный парсер: ✅ ДА
🚀 Запуск Playwright для браузерного парсинга...
🔧 Попытка 1: Загрузка С ПРОКСИ 250645a043088175:hkJHdx8M@res.proxy-seller.io:10002
🌐 Загрузка страницы: https://www.mexc.co/announcements/all
⏰ Таймаут при загрузке страницы
⚠️ Не удалось загрузить с прокси, пробуем БЕЗ прокси...
🔧 Попытка 2: Загрузка БЕЗ ПРОКСИ
🌐 Загрузка страницы: https://www.mexc.co/announcements/all
⏳ Ожидание загрузки JavaScript контента...
✅ Страница загружена (385147 байт)
📝 Стратегия: Поиск любого ключевого слова
   ✅ Найдено 1 ключевых слов
✅ Парсинг завершен: Найдены ключевые слова: fee
```

## Обновление от 15.01.2026 (Оптимизация скорости)

**Проблема**: Загрузка страницы занимала ~90+ секунд из-за тяжелого JavaScript.

**Решение**: 
1. **Изменен порядок попыток**: Сначала БЕЗ прокси (быстрее), потом с прокси
2. **Оптимизирована стратегия загрузки**: `networkidle` вместо `load`
   - Ждет, когда сетевая активность успокоится (нет новых запросов 500мс)
   - Более умная стратегия для SPA приложений типа MEXC
3. **Сокращено время ожидания**: 2 сек вместо 5 сек для React рендеринга
4. **Graceful timeout**: Даже при таймауте продолжает работу и получает контент

**Результат**: ⏱️ ~60-65 секунд (~1 минута) на проверку

Это нормальное время для таких тяжелых SPA-сайтов как MEXC. Страница успешно загружается и ключевые слова находятся корректно.

## Дата исправления
15 января 2026
