# 🔗 Извлечение прямых ссылок на анонсы

## 📋 Описание

Добавлена улучшенная функция для извлечения прямых ссылок на конкретные анонсы при парсинге страниц с объявлениями. Теперь при обнаружении ключевых слов бот не только уведомляет об изменениях, но и предоставляет:
- **Прямые ссылки** на найденные анонсы
- **Описание/превью** каждого анонса (первые 150-300 символов)
- **Поиск в родительских элементах** для более точного определения контекста

## ✨ Возможности

### 1. Автоматическое извлечение ссылок
- Поиск ссылок на анонсы, содержащие ключевые слова
- **Умный поиск**: проверка не только текста ссылки, но и всего родительского контейнера (article, div)
- Поддержка различных бирж (MEXC, Binance, Bybit, OKX, Gate.io, KuCoin)
- Умное определение структуры страницы для каждой биржи

### 2. Извлечение описания анонсов
- Автоматическое извлечение текста описания из родительского элемента
- Поддержка различных HTML-структур (React, статический HTML)
- Ограничение длины описания для компактности (150-300 символов)

### 3. Детальная информация об анонсах
Для каждого найденного анонса показывается:
- 📝 Заголовок анонса (до 200 символов)
- 🔗 Прямая ссылка на страницу анонса
- 📝 Описание/превью (первые 150 символов в уведомлениях)
- 🔑 Найденные ключевые слова

### 3. Интеграция во все режимы работы
- Автоматические проверки по расписанию
- Ручная проверка через веб-интерфейс
- Принудительная проверка через Telegram команду `/force_check`

## 🏗️ Техническая реализация

### Новый метод `_extract_announcement_links()`

```python
def _extract_announcement_links(self, soup: BeautifulSoup, keywords: List[str]) -> List[Dict[str, str]]:
    """
    Извлекает ссылки на анонсы, которые содержат указанные ключевые слова
    
    Улучшения:
    - Поиск в родительских контейнерах (article, div)
    - Извлечение описания из дочерних элементов
    - Умное определение структуры для каждой биржи
    
    Returns:
        [{'title': 'Заголовок', 
          'url': 'https://...', 
          'matched_keywords': ['keyword1'],
          'description': 'Описание...' (опционально)}]
    """
```

### Ключевые улучшения:

#### 1. **Поиск в родительских элементах**
```python
# Ищем родительский контейнер для полного контекста
parent = link.find_parent(['div', 'article', 'li', 'section'])
if parent:
    parent_text = parent.get_text(separator=' ', strip=True)
    # Проверяем ключевые слова в полном контексте
    search_text = (link_text + ' ' + parent_text).lower()
```

#### 2. **Извлечение описания**
```python
# Ищем описание в дочерних элементах
desc_elem = parent.find(['p', 'span', 'div'], recursive=True)
if desc_elem and desc_elem != link:
    description = desc_elem.get_text(strip=True)[:300]
```

#### 3. **Улучшенные селекторы для MEXC**
```python
'mexc.co': {
    'selectors': [
        'a[href*="/announcements/article/"]',  # Новый формат URL
        'a[href*="/support/articles/"]',
    ],
    'container_selectors': [
        'article', 
        'div[class*="article"]', 
        'div[class*="announcement"]'
    ],
    'url_prefix': 'https://www.mexc.co'
}
```

### Поддерживаемые биржи

| Биржа | CSS селекторы | Базовый URL |
|-------|--------------|-------------|
| **MEXC** | `a[href*="/support/articles/"]`, `.announcement-item` | `https://www.mexc.co` |
| **Binance** | `a[href*="/support/announcement/"]`, `.css-article a` | `https://www.binance.com` |
| **Bybit** | `a[href*="/announcements/"]`, `.announcement-list a` | `https://www.bybit.com` |
| **OKX** | `a[href*="/support/hc/"]`, `.article-item a` | `https://www.okx.com` |
| **Gate.io** | `a[href*="/article/"]`, `.article-link` | `https://www.gate.io` |
| **KuCoin** | `a[href*="/news/"]`, `.news-list a` | `https://www.kucoin.com` |

### Изменения в `_strategy_any_keyword()`

Метод теперь вызывает `_extract_announcement_links()` и добавляет найденные ссылки в результат:

```python
result = {
    'changed': True,
    'new_snapshot': None,
    'matched_content': f"Найдены ключевые слова: {', '.join(matched_keywords)}",
    'message': f'Найдены ключевые слова: {", ".join(matched_keywords)}',
    'announcement_links': announcement_links  # ← Новое поле
}
```

## 📱 Формат уведомлений

### До (старый формат):
```
📢 Обнаружены изменения в анонсах

📝 Ссылка: Mexc0%FEE
🔍 Стратегия: any_keyword
💬 Найдены ключевые слова: MEXC P2P

📄 Найдено:
Найдены ключевые слова: MEXC P2P

🔗 Открыть страницу
```

### После (новый формат с описанием):
```
📢 Обнаружены изменения в анонсах

📝 Ссылка: Mexc0%FEE
🔍 Стратегия: any_keyword
💬 Найдены ключевые слова: Unlock

📄 Найдено:
Найдены ключевые слова: Unlock

🎯 Найдено анонсов: 1

1. Unlock Your 2025 Cryptoverse Voyage: Invite & Share $55,000
   📝 Invite & Share $55,000 in Rewards. Dec 30, 2025. As 2025 winds down, revisit your year in crypto with MEXC...
   🔑 Unlock

🔗 Открыть страницу со всеми анонсами
```

## 🔧 Настройки

### Ограничения
- Максимум **10 ссылок** извлекается из HTML (для производительности)
- В уведомлениях показывается **топ-5** анонсов
- Заголовки анонсов обрезаются до **200 символов** (увеличено с 100)
- Описания обрезаются до **300 символов** в парсере, **150 символов** в уведомлениях

### Предотвращение дубликатов
Используется `set()` для отслеживания уже найденных URL и предотвращения дубликатов.

### Логирование
Добавлено детальное логирование:
- Количество найденных ссылок по каждому селектору
- Информация о найденных анонсах (заголовок, URL, ключевые слова, описание)
- Ошибки при обработке отдельных ссылок (debug уровень)

## 🧪 Тестирование

Созданы тестовые файлы:
1. [test_announcement_links.py](../tests/test_announcement_links.py) - Базовые тесты
2. [test_announcement_links_enhanced.py](../tests/test_announcement_links_enhanced.py) - **Расширенные тесты с описанием**

### Тесты в Enhanced версии:
- ✅ Извлечение ссылок с описанием из MEXC-подобного HTML
- ✅ Поиск в родительских элементах (article, div)
- ✅ Множественные ключевые слова
- ✅ Интеграция со стратегией any_keyword

### Запуск тестов:
```bash
# Базовые тесты
python dev/tests/test_announcement_links.py

# Расширенные тесты (с описанием)
python dev/tests/test_announcement_links_enhanced.py
```

## 📊 Примеры использования

### Пример 1: MEXC P2P анонсы
```python
parser = AnnouncementParser("https://www.mexc.co/announcements/all")
result = parser.parse(
    strategy='any_keyword',
    keywords=['MEXC P2P', 'fee', 'zero'],
    use_browser=True
)

if result['changed'] and result.get('announcement_links'):
    for link in result['announcement_links']:
        print(f"Найден: {link['title']}")
        print(f"URL: {link['url']}")
        print(f"Ключевые слова: {link['matched_keywords']}")
```

### Пример 2: Интеграция в уведомления
```python
if result.get('announcement_links'):
    announcement_links = result.get('announcement_links')
    message += f"🎯 Найдено анонсов: {len(announcement_links)}\n\n"
    
    for i, ann in enumerate(announcement_links[:5], 1):
        title = ann.get('title', 'Без названия')
        url = ann.get('url', '')
        keywords = ', '.join(ann.get('matched_keywords', []))
        
        message += f"{i}. <a href=\"{url}\">{title}</a>\n"
        if keywords:
            message += f"   🔑 {keywords}\n"
```

## 🚀 Преимущества

1. **Удобство** - Прямые ссылки на интересные анонсы без необходимости искать их вручную
2. **Скорость** - Быстрый переход к релевантным анонсам
3. **Контекст** - Видно, какие именно ключевые слова были найдены в каждом анонсе
4. **Масштабируемость** - Легко добавить поддержку новых бирж

## 🔮 Возможные улучшения

- [ ] Добавить кэширование найденных ссылок
- [ ] Фильтрация по дате публикации анонса
- [ ] Группировка анонсов по категориям
- [ ] Поддержка более сложных паттернов извлечения
- [ ] Автоматическое определение структуры страницы (ML)

## 📝 Changelog

**2026-01-15 (v2.0 - Enhanced)**
- ✅ Добавлен поиск в родительских элементах (article, div, section)
- ✅ Реализовано извлечение описания анонсов
- ✅ Улучшены селекторы для MEXC (`/announcements/article/`)
- ✅ Увеличен лимит заголовков до 200 символов
- ✅ Добавлено детальное логирование
- ✅ Создан расширенный тест `test_announcement_links_enhanced.py`

**2026-01-15 (v1.0)**
- ✅ Добавлен метод `_extract_announcement_links()`
- ✅ Обновлена стратегия `_strategy_any_keyword()`
- ✅ Интегрировано отображение ссылок во все типы уведомлений
- ✅ Добавлены тесты
- ✅ Создана документация

---

**Автор:** GitHub Copilot  
**Дата:** 15 января 2026  
**Версия:** 1.0
