# Инструкция по настройке парсинга стейкингов Gate.io

## Что реализовано

✅ **Полная поддержка парсинга стейкингов Gate.io**:
- Пагинация API (автоматический парсинг всех страниц)
- Фиксированные стейкинги (Fixed 7d, 14d, 30d и т.д.)
- Гибкие стейкинги (Flexible)
- Отображение заполненности пулов (fill_percentage)
- Фильтрация по APR (по умолчанию 20%)
- Автоматическая фильтрация sold out стейкингов
- Уведомления в формате как у Bybit (с прогресс-баром, лимитами)

## Как добавить ссылку на Gate.io

### Через бота:

1. Отправьте команду `/add` боту
2. Выберите **"Стейкинг"**
3. Заполните данные:
   ```
   Название: Gate.io Staking

   API URL: https://www.gate.com/apiw/v2/uni-loan/earn/market/list

   Страница для пользователя: https://www.gate.com/ru/simple-earn

   Минимальный APR: 20

   Биржа: Gate.io
   ```

4. Подтвердите создание ссылки

### Параметры:

| Параметр | Значение | Описание |
|----------|----------|----------|
| **API URL** | `https://www.gate.com/apiw/v2/uni-loan/earn/market/list` | Основной URL API |
| **Page URL** | `https://www.gate.com/ru/simple-earn` | Ссылка для пользователя |
| **Min APR** | `20` (или любое другое значение) | Минимальный APR для фильтрации |
| **Exchange** | `Gate.io` или `gate` | Название биржи (автоопределяется) |
| **Category** | `staking` | Категория ссылки |
| **Parsing Type** | `api` | Тип парсинга |

## Как работает парсер

### 1. Пагинация
Парсер автоматически проходит все страницы API (page 1, 2, 3...) до тех пор, пока не закончатся данные.

### 2. Типы стейкингов

#### Fixed (Фиксированные)
- **Lock Period**: 7, 14, 30, 90, 120 дней
- **APR**: из поля `year_rate`
- **Заполненность**: рассчитывается из `total_lend_amount / total_lend_all_amount * 100`

#### Flexible (Гибкие)
- **Lock Period**: 0 дней (без блокировки)
- **APR**: максимальный из `max_year_rate` и `ladder_apr`
- **Bonus**: может быть бонус в другой монете (GT)

### 3. Фильтрация

Парсер автоматически фильтрует:
- ✅ **sale_status = 1** (только активные)
- ✅ **APR >= min_apr** (по умолчанию 20%)
- ✅ **total_lend_available > 0** (есть места, не sold out)
- ✅ **VIP продукты** (помечаются отдельно)
- ✅ **Promotional** (акционные предложения)

### 4. Данные о заполненности

Для каждого стейкинга показывается:
- **Общий пул** (max_capacity): `total_lend_all_amount`
- **Занято** (current_deposit): `total_lend_amount`
- **Доступно**: `total_lend_available`
- **Заполненность** (fill_percentage): `(занято / общий пул) * 100%`
- **Прогресс-бар**: визуальная шкала заполненности

## Примеры уведомлений

### Фиксированный стейкинг (100% APR)
```
🆕 НОВЫЙ СТЕЙКИНГ!

💎 Монета: USDT
🏦 Биржа: Gate.io
💰 APR: 100%
📅 Период: 3 дней
🔧 Тип: Fixed 3d
📊 Статус: Active
🏷️ Категория: Limited-Time Event - New users enjoy 100% APR

📈 Заполненность: 32.29%
▓▓▓▓▓▓░░░░░░░░░░░░░░

💎 ЛИМИТЫ И ЗАПОЛНЕННОСТЬ:
   • Общий пул: 721,631,474.65 USDT ($721,631,474)
   • Занято: 233,008,680.28 USDT (32.29%)
   • Осталось: 488,622,794.37 USDT (~$488,622,794)
   • Лимит/аккаунт: 500 USDT (~$500)

🔗 Ссылка: https://www.gate.com/ru/simple-earn
```

### Гибкий стейкинг с бонусом (94% APR)
```
🆕 НОВЫЙ СТЕЙКИНГ!

💎 Монета: 0G
🏦 Биржа: Gate.io
💰 APR: 94.27%
📅 Период: Flexible (бессрочно)
🔧 Тип: Flexible
📊 Статус: Active
🏷️ Категория: Bonus: GT

📈 Заполненность: 45.12%
▓▓▓▓▓▓▓▓▓░░░░░░░░░░░

🔗 Ссылка: https://www.gate.com/ru/simple-earn
```

## Текущие стейкинги

Как и у Bybit, для Gate.io доступна функция **"Текущие стейкинги"**:

1. Боту отправить команду `/manage`
2. Выбрать вашу ссылку Gate.io
3. Нажать **"Текущие стейкинги"**

Бот покажет:
- Все активные стейкинги с текущей заполненностью
- Прогресс-бар для каждого пула
- Динамику изменения APR и заполненности (если есть история)
- Фильтр по APR (можно изменить)

## Редактирование фильтра APR

1. Команда `/manage` → выбрать ссылку Gate.io
2. Нажать **"⚙️ Настройки фильтра APR"**
3. Установить новое значение (например, 15% вместо 20%)
4. Сохранить

Бот начнет показывать стейкинги с APR >= 15%.

## Тестирование

Для проверки работоспособности парсера:

```bash
# Быстрый тест (без запроса цен)
python dev/tests/test_gate_quick.py

# Полный тест (с запросом цен токенов)
python dev/tests/test_gate_parser.py
```

## Технические детали

### Структура API ответа

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "list": [
      {
        "asset": "USDT",
        "name": "Tether",
        "total_lend_amount": "233008680.28",      // Занято
        "total_lend_available": "488622794.36",   // Доступно
        "total_lend_all_amount": "721631474.65",  // Общий пул
        "year_rate": "0.1076",                    // Базовый APR (10.76%)
        "max_year_rate": "0.1076",                // Максимальный APR
        "fixed_list": [                           // Фиксированные стейкинги
          {
            "id": 301,
            "lock_up_period": 3,                  // Период блокировки (дни)
            "sale_status": 1,                     // 1 = активен
            "year_rate": "1",                     // APR (100%)
            "user_max_lend_volume": "500",        // Лимит на пользователя
            "title": "Limited-Time Event",
            "subtitle": "New users enjoy 100% APR"
          }
        ],
        "fixable_list": [                         // Гибкие стейкинги
          {
            "id": 1,
            "year_rate": "0.0176",                // Базовый APR
            "max_year_rate": "0.1076",            // Макс APR (с бонусами)
            "bonus_asset": "GT",                  // Бонусная монета
            "ladder_apr": [                       // Ступенчатые ставки
              {"left": "0", "right": "500", "apr": "0.09"}
            ]
          }
        ]
      }
    ]
  }
}
```

### Маппинг полей

| Gate.io API | Наша БД | Описание |
|-------------|---------|----------|
| `asset` | `coin` | Символ монеты (USDT, BTC) |
| `year_rate` * 100 | `apr` | APR в процентах |
| `lock_up_period` | `term_days` | Период блокировки |
| `sale_status = 1` | `status = 'Active'` | Статус стейкинга |
| `total_lend_all_amount` | `max_capacity` | Общий пул |
| `total_lend_amount` | `current_deposit` | Занято |
| `total_lend_available` | — | Доступно (для фильтрации) |
| `user_max_lend_volume` | `user_limit_tokens` | Лимит на пользователя |

## Проблемы и решения

### CoinGecko Rate Limit
При массовом парсинге может срабатывать rate limit CoinGecko API (429 Too Many Requests).

**Решение**: Цены токенов запрашиваются с повторными попытками и кешированием. Если цена недоступна, стейкинг все равно сохраняется, просто без USD эквивалента.

### Ошибка "NoneType object is not iterable"
Возникает когда `fixed_list` или `fixable_list` равен `None` вместо пустого массива.

**Решение**: В коде используется `.get('fixed_list', [])` для безопасного получения списков.

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `main.py` выводит детальную информацию о парсинге
2. Запустите тесты: `python dev/tests/test_gate_quick.py`
3. Проверьте что API URL правильный и доступен

---

**Автор**: Claude Sonnet 4.5
**Дата**: 2026-01-12
**Версия**: 1.0
