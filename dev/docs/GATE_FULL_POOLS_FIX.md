# Исправление: Полностью заполненные стейкинги Gate.io

**Дата**: 2026-01-12
**Проблема**: Стейкинги Gate.io с заполненностью 100% показывались как доступные

## Описание проблемы

В списке "📈 ТЕКУЩИЕ СТЕЙКИНГИ" отображались полностью заполненные стейкинги Gate.io, хотя в них невозможно войти.

### Пример проблемы

```
💰 GMT | 296.7% APR | Flexible
📊 Статус: ✅ Active
▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ 100.00%

💎 ЛИМИТЫ И ЗАПОЛНЕННОСТЬ:
   • Общий пул: 8,441,183.15 GMT
   • Занято: 8,441,183.15 GMT (100.00%)
   • Осталось: 0.00 GMT  ← В него невозможно войти!
```

**Почему это плохо**: Пользователь видит стейкинг, открывает биржу, но не может зайти в него.

## Причина

### Неправильный расчет доступности

Код парсера `parsers/staking_parser.py` использовал **только математический расчет**:

```python
# СТАРЫЙ КОД (неправильный)
total_lend_amount = float(coin_data.get('total_lend_amount', 0))
total_lend_all_amount = float(coin_data.get('total_lend_all_amount', 0))

if total_lend_all_amount > 0:
    fill_percentage = round((total_lend_amount / total_lend_all_amount) * 100, 2)
```

### Почему это не работает

Gate.io API предоставляет поле **`total_lend_available`**, которое показывает **реальную** доступность:

```json
{
  "asset": "GMT",
  "total_lend_amount": "8440844.53",      // Занято
  "total_lend_available": "0",            // ← ДОСТУПНО = 0!
  "total_lend_all_amount": "8441183.15"   // Общий лимит
}
```

**Математически**: Осталось `8441183.15 - 8440844.53 = 338.62 GMT` (0.004%)
**Реально**: `total_lend_available = 0` → стейкинг **НЕДОСТУПЕН**

**Почему?**
- Gate.io имеет минимальный порог входа
- Остаток 0.004% слишком мал для входа
- API корректно возвращает `total_lend_available = 0`

## Решение

### Фильтрация на уровне парсера

Добавлена проверка поля `total_lend_available` **до** сохранения стейкинга в БД.

#### Для гибких стейкингов (fixable_list)

**Файл**: `parsers/staking_parser.py:860-865`

```python
# Заполненность (из coin_data)
total_lend_amount = float(coin_data.get('total_lend_amount', 0))
total_lend_available = float(coin_data.get('total_lend_available', 0))
total_lend_all_amount = float(coin_data.get('total_lend_all_amount', 0))

# ВАЖНО: Проверяем доступность по полю total_lend_available
# Если Gate.io API возвращает total_lend_available=0, значит стейкинг недоступен
# даже если математически остается небольшой остаток
if total_lend_available <= 0:
    logger.debug(f"🔽 Gate.io: пропущен недоступный гибкий продукт {coin} (total_lend_available={total_lend_available})")
    return None  # Не сохраняем в БД
```

#### Для фиксированных стейкингов (fixed_list)

**Файл**: `parsers/staking_parser.py:735-739`

```python
# ВАЖНО: Проверяем доступность по полю total_lend_available
# Если Gate.io API возвращает total_lend_available=0, значит стейкинг недоступен
if total_lend_available <= 0:
    logger.debug(f"🔽 Gate.io: пропущен недоступный фиксированный продукт {coin} (total_lend_available={total_lend_available})")
    return None
```

## Почему фильтруем на уровне парсера, а не UI?

### ✅ Правильно: Фильтрация в парсере
1. **Не сохраняем в БД** недоступные стейкинги
2. **Не создаем снимки** (snapshots) для недоступных стейкингов
3. **Меньше нагрузка** на БД и API цен токенов
4. **Не отправляем уведомления** о "новых" стейкингах, которые недоступны

### ❌ Неправильно: Фильтрация в UI
1. Недоступные стейкинги сохраняются в БД
2. Создаются снимки для них
3. Запрашиваются цены токенов
4. Могут отправиться уведомления (если они были новыми)
5. Занимают место в базе данных

## Результаты тестирования

### До исправления
```bash
INFO:__main__:📊 Найдено 45 стейкингов для GateStaking
# Включая GMT (100% заполнен)
```

### После исправления
```bash
INFO:parsers.staking_parser:🔽 Gate.io: пропущен недоступный гибкий продукт GMT (total_lend_available=0.0)
INFO:__main__:📊 Найдено 44 стейкингов для GateStaking
# GMT исключен на уровне парсинга
```

## Дополнительная информация

### Структура Gate.io API ответа

```json
{
  "data": {
    "list": [
      {
        "asset": "GMT",
        "total_lend_amount": "8440844.53",      // Сколько застейкано
        "total_lend_available": "0",            // Доступно для стейкинга (ВАЖНО!)
        "total_lend_all_amount": "8441183.15",  // Общий лимит
        "fixable_list": [
          {
            "id": 396,
            "sale_status": 1,  // 1 = Active
            "year_rate": "2.9667"
          }
        ]
      }
    ]
  }
}
```

### Логика доступности

| Условие | Действие |
|---------|----------|
| `total_lend_available > 0` | ✅ Парсим и сохраняем стейкинг |
| `total_lend_available <= 0` | 🔽 Пропускаем (return None) |
| `sale_status != 1` | 🔽 Пропускаем (неактивный) |

## Применимость к другим биржам

### Bybit
Bybit использует другую логику:
- `display_status = 2` означает "Sold Out"
- `max_capacity` и `current_deposit` для расчета заполненности
- Не нужны изменения

### Kucoin
Kucoin публичный API **не предоставляет** данных о заполненности.
- Не показывает `max_capacity` или `current_deposit`
- Нельзя определить доступность на уровне парсера
- Не нужны изменения

### OKX
OKX Flash Earn не предоставляет общий лимит пула.
- Только `user_limit` (лимит на пользователя)
- Нельзя точно рассчитать заполненность
- Не нужны изменения

## Заключение

✅ **Проблема решена**: Недоступные стейкинги Gate.io больше не показываются
✅ **Оптимизация**: Фильтрация на уровне парсера снижает нагрузку на систему
✅ **Точность**: Используется официальное поле API `total_lend_available`

---

**Тестирование**: `python dev/tests/test_gate_api_structure.py`
**Автор**: Claude Sonnet 4.5
**Версия**: 1.0
