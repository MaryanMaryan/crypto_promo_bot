# Обновление порога фильтрации стейкингов GateStaking

**Дата**: 16.01.2026
**Изменение**: Порог фильтрации изменен с 100% на 95%

## Описание проблемы

Стейкинги GateStaking с заполненностью 99%+ показывались пользователям, но в них оставалось очень мало места (например, 99.85 BREV или 134.60 FLOW). Это нецелесообразно, так как:

- Высокая вероятность, что пул заполнится до того, как пользователь успеет войти
- Малые остатки могут не покрыть минимальный депозит
- Создается ложное впечатление о доступности стейкинга

## Решение

Изменен порог фильтрации с **100%** на **95%**. Теперь стейкинги с заполненностью **≥95%** не показываются пользователям.

## Измененные файлы

### 1. Основной парсер
**Файл**: `parsers/staking_parser.py`
- Строка ~803: Фильтрация при парсинге общего списка Gate.io
- Комментарий обновлен: "Скрываем стейкинги с заполненностью >= 95%"

### 2. Сервис снэпшотов
**Файл**: `services/staking_snapshot_service.py`
- Строка ~193: Фильтр SQL-запроса изменен с `< 100.0` на `< 95.0`
- Комментарий обновлен: "Исключаем стейкинги с заполненностью >= 95%"

### 3. Обработчики бота
**Файл**: `bot/handlers.py`
- Строка ~2693: Фильтр для проверки пулов изменен с `< 100` на `< 95`
- Строка ~2702: Фильтр для статистики sold out изменен с `>= 100` на `>= 95`
- Строка ~2738: Счетчик sold out обновлен с `>= 100` на `>= 95`
- Комментарии обновлены: "пулы с заполненностью >= 95%"

### 4. Скрипты очистки
**Файлы**: 
- `temp_cleanup.py`
- `dev/scripts/cleanup_full_stakings.py`

Обновлены для консистентности с новым порогом 95%.

### 5. Тестовые скрипты
**Файл**: `dev/scripts/show_staking_examples.py`
- Строка ~121: Фильтр изменен с `< 100` на `< 95`
- Строка ~125: Описание обновлено: "заполненность <95%"

## Примеры

### До изменения (порог 100%)
```
💰 BREV | 499.0% APR | Flexible
💎 ЗАПОЛНЕННОСТЬ ПУЛА:
   ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░ 99.98%
   • Общий пул: 490,048.09 BREV
   • Занято: 489,948.24 BREV (99.98%)
   • Осталось: 99.85 BREV  ← Показывался (нецелесообразно)
```

### После изменения (порог 95%)
```
💰 BREV | 499.0% APR | Flexible
💎 ЗАПОЛНЕННОСТЬ ПУЛА:
   ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░ 99.98%
   ❌ НЕ ПОКАЗЫВАЕТСЯ (заполненность >= 95%)
```

## Логика работы

### Парсер (staking_parser.py)
```python
if fill_percentage >= 95:
    logger.debug(f"🔽 Gate.io: пропущена монета {coin} (заполненность={fill_percentage}%)")
    continue
```

### Сервис снэпшотов (staking_snapshot_service.py)
```python
query = query.filter(
    or_(
        StakingHistory.fill_percentage == None,
        StakingHistory.fill_percentage < 95.0
    )
)
```

### Обработчики бота (handlers.py)
```python
pools_with_fill = [
    s for s in stakings
    if s.get('fill_percentage') is not None
    and s.get('apr', 0) >= 100
    and s.get('fill_percentage', 0) < 95  # Не заполненные >= 95%
    and s.get('status') != 'Sold Out'
]
```

## Влияние на другие биржи

### Bybit
- Не затронут (использует свой механизм `display_status = 2` для Sold Out)
- Заполненность считается отдельно

### Kucoin
- Не затронут (нет данных о заполненности в API)

### OKX
- Не затронут (нет данных об общем пуле в API)

### Gate.io
- ✅ **Затронут** - основная цель изменения
- Теперь фильтрует стейкинги с заполненностью ≥95%

## Преимущества

1. ✅ **Более реалистичная информация** - показываются только стейкинги с достаточным остатком
2. ✅ **Меньше разочарований** - пользователи не увидят "почти заполненные" пулы
3. ✅ **Лучший UX** - фокус на доступных возможностях
4. ✅ **Консистентность** - единый порог во всех частях системы

## Недостатки

1. ⚠️ **Может скрыть доступные пулы** - если остаток 5% от большого пула, это может быть значительная сумма
2. ⚠️ **Меньше вариантов** - уменьшается количество показываемых стейкингов

## Альтернативные подходы

### Вариант 1: Динамический порог
```python
# Порог зависит от размера остатка в USD
available_usd = available_tokens * token_price
if available_usd < 1000:  # Меньше $1000
    skip = True
```

**Плюсы**: Учитывает реальную ценность остатка
**Минусы**: Сложнее в реализации, требует цены токена

### Вариант 2: Предупреждение вместо скрытия
```
⚠️ ПОЧТИ ЗАПОЛНЕН (95%+)
Осталось: 99.85 BREV
Успейте войти!
```

**Плюсы**: Пользователь видит все варианты
**Минусы**: Загромождает список, может вводить в заблуждение

### Вариант 3: Настраиваемый порог
```python
# В настройках бота
user_settings['pool_fill_threshold'] = 95  # От 80 до 100
```

**Плюсы**: Гибкость для пользователя
**Минусы**: Усложняет интерфейс и код

## Тестирование

### Ручное тестирование
1. Запустить парсер Gate.io: `python main.py`
2. Проверить логи: должны появиться сообщения `"🔽 Gate.io: пропущена монета ... (заполненность=...)"`
3. Проверить список стейкингов: должны отсутствовать стейкинги с заполненностью ≥95%

### Проверка в боте
1. Отправить команду `/check_stakings`
2. Выбрать GateStaking
3. Убедиться, что показываются только стейкинги с заполненностью <95%

## Заключение

✅ **Изменение успешно внедрено** во все необходимые части системы
✅ **Консистентность** - единый порог 95% во всех модулях
✅ **Улучшен UX** - пользователи видят только релевантные стейкинги
⚠️ **Мониторинг** - следить за feedback от пользователей

---

**Версия**: 1.0  
**Автор**: GitHub Copilot  
**Связанные документы**: 
- `GATE_FULL_POOLS_FIX.md` - предыдущее исправление для 100% пулов
- `GATE_IO_SETUP.md` - общая документация по Gate.io
