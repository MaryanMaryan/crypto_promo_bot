# 🎨 Обновление форматирования стейкингов v2.0

## 📋 Общие требования

### APR-индикаторы (вместо 💎)
| APR | Эмодзи |
|-----|--------|
| > 300% | 🔥🔥🔥 |
| > 150% | 🔥🔥 |
| > 50% | 🔥 |
| ≤ 50% | 💎 |

### Заголовки
- **Новый стейкинг**: `🔥🔥🔥 EXCHANGE STAKING` (эмодзи по APR)
- **Стабилизированный**: `🕐 СТАБИЛИЗИРОВАН | EXCHANGE STAKING`

### Общие правила
- [x] Заполненность пула - НЕ показывать для новых/стабилизированных
- [x] Ссылка - просто URL без "Перейти к стейкингу"
- [x] Время стабилизации - из настроек пользователя (`stability_hours`)
- [x] Fixed - показывать разные суммы инвестиций
- [x] Flexible - показывать разные периоды (1/7/14 дней)

---

## 📝 ЗАДАЧИ

### 1. Функция определения APR-эмодзи
**Файл**: `bot/notification_service.py`
**Метод**: Добавить `get_apr_emoji(apr: float) -> str`

```python
def get_apr_emoji(self, apr: float) -> str:
    """Возвращает эмодзи в зависимости от уровня APR"""
    if apr > 300:
        return "🔥🔥🔥"
    elif apr > 150:
        return "🔥🔥"
    elif apr > 50:
        return "🔥"
    else:
        return "💎"
```

- [ ] Создать метод `get_apr_emoji`
- [ ] Использовать в заголовках вместо фиксированного 💎

---

### 2. Обновить `format_new_staking` - НОВЫЕ стейкинги
**Файл**: `bot/notification_service.py`

#### 2.1 Общий формат (базовый)
```
🔥🔥 EXCHANGE STAKING

🪙 COIN
📈 APR: XXX%
⏳ Срок: X дней / Flexible

💰 Потенциальный доход:
├─ $100 → +$X.XX за X дн.
├─ $500 → +$X.XX за X дн.
└─ $1000 → +$X.XX за X дн.

👤 Лимит: X,XXX COIN (~$X,XXX)

https://exchange.com/earn
```

- [ ] Заменить `💎` на динамический `get_apr_emoji(apr)`
- [ ] Убрать прогресс-бар заполненности
- [ ] Заменить `🔗 <a href="...">Перейти</a>` на просто URL

---

#### 2.2 Bybit USDT стейкинг (специальный формат)
**Условие**: `exchange == 'Bybit' AND coin == 'USDT'`

```
🔥🔥🔥 BYBIT STAKING | USDT

📈 555% APR
👤 Только для новых
⏳ Срок: 2 дня

💰 Расчёт дохода:
├─ $100 → +$3.04
├─ $200 → +$6.08
└─ $300 → +$9.12

https://www.bybit.com/earn
```

**Особенности:**
- [ ] Суммы фиксированные: `$100 / $200 / $300`
- [ ] Показывать `👤 Только для новых` если `is_new_user == True`
- [ ] Показывать `👑 VIP` если `is_vip == True`
- [ ] Показывать `🌍 Регион: X` если `regional_tag` есть

---

#### 2.3 Flexible стейкинг (расчёт по периодам)
**Условие**: `term_days == 0`

```
🔥🔥 MEXC STAKING

🪙 USAT
📈 300% APR
⏳ Flexible

💰 Доход с $1000:
├─ 1 День: +$8.22
├─ 7 Дней: +$57.53
└─ 14 Дней: +$115.07

👤 Лимит: 100,000 USAT (~$100,160)

https://www.mexc.com/earn
```

- [ ] Для Flexible показывать периоды: 1 / 7 / 14 дней
- [ ] Расчёт: `$1000 * (APR / 100) * (days / 365)`

---

#### 2.4 Fixed стейкинг (расчёт по суммам)
**Условие**: `term_days > 0`

```
💎 KUCOIN STAKING

🪙 BDX
📈 150% APR
⏳ Срок: 30 дней

💰 Потенциальный доход:
├─ $100 → +$12.33 за 30 дн.
├─ $500 → +$61.64 за 30 дн.
└─ $1000 → +$123.29 за 30 дн.

https://www.kucoin.com/earn
```

- [ ] Для Fixed показывать суммы: $100 / $500 / $1000
- [ ] Расчёт: `amount * (APR / 100) * (term_days / 365)`

---

#### 2.5 Gate.io Combined (Fixed + Flexible)
**Условие**: `type == 'Fixed/Flexible'` или есть `fixed_apr` и `flexible_apr`

```
🔥🔥 GATE.IO STAKING | FLOW

📊 Fixed: 180% APR (30 дней)
📊 Flexible: 152.96% APR

💰 Доход с $1000 (Fixed 30д):
└─ +$147.95

💰 Доход с $1000 (Flexible):
├─ 1 День: +$4.19
├─ 7 Дней: +$29.33
└─ 14 Дней: +$58.66

👤 Лимит: 2.1M FLOW (~$157,639)

https://www.gate.io/hodl
```

- [ ] Показывать оба APR (Fixed и Flexible)
- [ ] Отдельный расчёт дохода для каждого типа
- [ ] APR-эмодзи по максимальному из двух

---

### 3. Обновить формат для СТАБИЛИЗИРОВАННЫХ стейкингов
**Файл**: `bot/notification_service.py`

#### 3.1 Стабилизированный Flexible
```
🕐 СТАБИЛИЗИРОВАН | MEXC STAKING

🪙 USAT
📈 300% APR
⏳ Flexible
⏱️ Стабилизация: 6 ч.

💰 Доход с $1000:
├─ 1 День: +$8.22
├─ 7 Дней: +$57.53
└─ 14 Дней: +$115.07

👤 Лимит: 100,000 USAT (~$100,160)

https://www.mexc.com/earn
```

- [ ] Заголовок: `🕐 СТАБИЛИЗИРОВАН | EXCHANGE STAKING`
- [ ] Добавить строку `⏱️ Стабилизация: X ч.` из настроек пользователя
- [ ] Получать `stability_hours` из `user_settings` или `link_settings`

---

#### 3.2 Стабилизированный Gate Combined
```
🕐 СТАБИЛИЗИРОВАН | GATE.IO STAKING | FLOW

📊 Fixed: 180% APR (30 дней)
📊 Flexible: 152.96% APR
⏱️ Стабилизация: 6 ч.

💰 Доход с $1000 (Fixed 30д):
└─ +$147.95

💰 Доход с $1000 (Flexible):
├─ 1 День: +$4.19
├─ 7 Дней: +$29.33
└─ 14 Дней: +$58.66

👤 Лимит: 2.1M FLOW (~$157,639)

https://www.gate.io/hodl
```

- [ ] Аналогичный Combined формат
- [ ] Добавить время стабилизации

---

### 4. Передача параметра is_stabilized в форматтер
**Файлы**: 
- `services/stability_tracker_service.py`
- `bot/notification_service.py`

- [ ] Добавить параметр `is_stabilized: bool = False` в `format_new_staking`
- [ ] Добавить параметр `stability_hours: int = None` в `format_new_staking`
- [ ] При вызове из stability_tracker передавать `is_stabilized=True` и `stability_hours`

---

### 5. Получение stability_hours из настроек
**Файл**: `services/stability_tracker_service.py`

- [ ] При отправке стабилизированного стейкинга получать `stability_hours` из настроек ссылки
- [ ] Передавать в `format_new_staking`

```python
# Пример
link_settings = await db.get_link_settings(link_id)
stability_hours = link_settings.get('stability_hours', 6)  # default 6
```

---

### 6. URL-ссылки на страницы стейкингов
**Файл**: `bot/notification_service.py` или `config/url_templates.json`

| Биржа | URL |
|-------|-----|
| Bybit | https://www.bybit.com/earn |
| Kucoin | https://www.kucoin.com/earn |
| Gate.io | https://www.gate.io/hodl |
| MEXC | https://www.mexc.com/earn |
| OKX | https://www.okx.com/earn |
| Binance | https://www.binance.com/earn |
| Bitget | https://www.bitget.com/earn |

- [ ] Создать словарь `STAKING_URLS` с URL для каждой биржи
- [ ] Использовать в форматтере вместо `page_url`

---

### 7. Рефакторинг метода `format_new_staking`

**Итоговая структура:**

```python
def format_new_staking(
    self, 
    staking: Dict[str, Any], 
    page_url: str = None,
    is_stabilized: bool = False,
    stability_hours: int = None
) -> str:
    """
    Форматирование уведомления о новом/стабилизированном стейкинге
    
    Форматы:
    1. Bybit USDT - специальный (суммы $100/$200/$300)
    2. Fixed - расчёт для сумм $100/$500/$1000
    3. Flexible - расчёт для периодов 1/7/14 дней
    4. Combined (Gate) - оба APR с расчётами
    """
    
    # 1. Определяем APR-эмодзи
    apr_emoji = self.get_apr_emoji(apr)
    
    # 2. Формируем заголовок
    if is_stabilized:
        header = f"🕐 СТАБИЛИЗИРОВАН | {exchange.upper()} STAKING"
    else:
        header = f"{apr_emoji} {exchange.upper()} STAKING"
    
    # 3. Добавляем монету в заголовок для Combined
    if is_combined or coin in ['USDT', 'USDC']:
        header += f" | {coin}"
    
    # 4. Выбираем формат расчёта
    if exchange == 'Bybit' and coin == 'USDT':
        # Bybit USDT: $100/$200/$300
        amounts = [100, 200, 300]
    elif is_combined:
        # Gate Combined: отдельные расчёты
        ...
    elif term_days == 0:
        # Flexible: периоды 1/7/14 дней
        periods = [1, 7, 14]
    else:
        # Fixed: суммы $100/$500/$1000
        amounts = [100, 500, 1000]
    
    # 5. Добавляем время стабилизации (только для стабилизированных)
    if is_stabilized and stability_hours:
        message += f"⏱️ Стабилизация: {stability_hours} ч.\n"
    
    # 6. НЕ добавляем прогресс-бар заполненности
    
    # 7. URL вместо кликабельной ссылки
    staking_url = STAKING_URLS.get(exchange.lower(), page_url)
    if staking_url:
        message += f"\n{staking_url}"
```

---

## 🧪 Тестирование

### Тест-кейсы:

1. [ ] **Bybit USDT новый** - 555% APR, 2 дня, new_user
2. [ ] **Bybit USDT стабилизированный** - с временем стабилизации
3. [ ] **MEXC Flexible новый** - 300% APR
4. [ ] **MEXC Flexible стабилизированный** - с временем стабилизации
5. [ ] **Kucoin Fixed** - 150% APR, 30 дней
6. [ ] **Gate Combined новый** - Fixed 180% + Flexible 153%
7. [ ] **Gate Combined стабилизированный**
8. [ ] **Низкий APR** (≤50%) - проверить эмодзи 💎

---

## 📁 Файлы для изменения

1. `bot/notification_service.py`
   - `get_apr_emoji()` - новый метод
   - `format_new_staking()` - полный рефакторинг
   - `STAKING_URLS` - словарь URL

2. `services/stability_tracker_service.py`
   - Передача `is_stabilized=True` и `stability_hours`

3. `config/url_templates.json` (опционально)
   - Добавить URL стейкингов

---

## 📅 Приоритет выполнения

1. **HIGH**: `get_apr_emoji()` - базовая функция
2. **HIGH**: Рефакторинг `format_new_staking()` 
3. **MEDIUM**: Gate Combined формат
4. **MEDIUM**: Передача `stability_hours` из настроек
5. **LOW**: URL-ссылки в config

---

## ✅ Критерии готовности

- [ ] APR-эмодзи работает корректно
- [ ] Bybit USDT показывает $100/$200/$300
- [ ] Flexible показывает 1/7/14 дней
- [ ] Fixed показывает $100/$500/$1000
- [ ] Gate Combined показывает оба APR
- [ ] Стабилизированные имеют заголовок 🕐
- [ ] Время стабилизации берётся из настроек
- [ ] Нет прогресс-бара заполненности
- [ ] URL вместо кликабельной ссылки
