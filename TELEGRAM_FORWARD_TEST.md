# Тестирование пересылки сообщений из Telegram

## Что изменилось:

### ✅ Автоматический мониторинг (TelegramMonitor)
**Файл:** `services/telegram_monitor.py`

Теперь вместо составления своего сообщения бот **пересылает оригинальное** из канала:
- Сохраняется оригинальное форматирование
- Все эмодзи, ссылки и детали промоакции
- Нет ошибок парсинга

**Формат уведомления:**
```
🔔 Новая промоакция из канала @WEEXNews
🔑 Ключевые слова: Airdrop, Trading Campaign
━━━━━━━━━━━━━━━━
[Оригинальное сообщение из канала пересылается]
```

### ⚠️ Принудительная проверка
Для принудительной проверки пока используется старый формат (составное сообщение), так как:
- Сообщения получаются через API `get_recent_messages()` без event context
- Нет прямого доступа к объекту message для пересылки

**Возможное улучшение:**
1. Сохранять полный текст (не обрезать до 500 символов)
2. Отправлять как quote с сохранением форматирования

## Как тестировать:

### 1. Запустить автоматический мониторинг

```bash
# В .env
TELEGRAM_PARSER_ENABLED=true

# Запустить бота
python main.py
```

### 2. Дождаться нового сообщения в @WEEXNews

Когда в канале появится новое сообщение с ключевыми словами:
- `Airdrop`
- `Trading Campaign`
- `claim`
- `Event Details`
- `worth of`

Бот:
1. Отправит заголовок с информацией
2. **Переслать оригинальное сообщение**

### 3. Проверить результат

Вы получите:
```
🔔 Новая промоакция из канала @WEEXNews
🔑 Ключевые слова: Airdrop, Trading Campaign
━━━━━━━━━━━━━━━━

[Forwarded from WEEX News]
📣 #WEEX Exclusive Airdrop Alert!

New users can claim up to $50,000 worth of $0G in the Trading Campaign!

📅26/12/2025, 10:00:00 - 02/01/2026, 10:00:00 (UTC+1)

🔗 Event Details: https://app.sensor.weex.tech:8106/t/iea
📝Register: https://bit.ly/44qKAwK
...
```

## Изменения в коде:

### services/telegram_monitor.py

**Строка 175-179:**
```python
# Отправляем уведомление (пересылаем оригинальное сообщение)
await self.send_notification(
    channel_username,
    message,  # Передаем весь message объект для пересылки
    result
)
```

**Строка 245-300:**
```python
async def send_notification(self, channel_username: str, message, result: Dict):
    # Если message - это объект Telethon Message, пересылаем его
    if hasattr(message, 'id') and hasattr(message, 'chat'):
        # Сначала отправляем заголовок
        header = f"🔔 <b>Новая промоакция из канала @{channel_username}</b>\n"
        # ... ключевые слова ...

        await self.bot.send_message(...)

        # Затем пересылаем оригинальное сообщение
        await self.bot.forward_message(
            config.ADMIN_CHAT_ID,
            message.chat.id,
            message.id
        )
```

## Альтернатива для принудительной проверки

Если хотите также пересылать для принудительной проверки:

### Вариант 1: Сохранить полный текст
В `parsers/universal_fallback_parser.py:366`:
```python
'description': msg['text'],  # Вместо msg['text'][:500]
```

### Вариант 2: Отправлять как quote
В `bot/notification_service.py` добавить опцию для Telegram промоакций:
```python
if promo.get('data_source') == 'telegram':
    # Отправить полный текст без обрезки
    await bot.send_message(
        chat_id,
        promo['description'],
        parse_mode=None  # Сохранить оригинальное форматирование
    )
```

## Преимущества пересылки:

✅ **Сохраняется форматирование** - жирный текст, ссылки, эмодзи
✅ **Нет ошибок парсинга** - не нужно вручную извлекать данные
✅ **Полная информация** - ничего не теряется
✅ **Видно источник** - "Forwarded from WEEX News"
✅ **Кликабельные ссылки** - сразу готовы к использованию

## Следующие шаги:

1. Запустить автоматический мониторинг
2. Дождаться новых сообщений в @WEEXNews
3. Проверить, что приходят пересланные сообщения
4. При желании - улучшить принудительную проверку

Готово! 🎉
